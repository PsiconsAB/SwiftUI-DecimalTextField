A reusable SwiftUI component for numeric input that:
- Accepts both `.` and `,` as decimal separators
- Uses `NumberFormatter` for display and validation
- Blocks invalid characters
- Formats on edit end for clean UX
  Took a while but I believe it realy works. Had som help from Mr. Musk (Grok)
